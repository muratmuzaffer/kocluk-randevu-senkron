# Vercel middleware (canlı site girişi) — değerleri Vercel Dashboard / CLI ile verin
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=

# Kitap Slayt (/kitap-slayt) — Gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
